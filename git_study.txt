git init

git config --local user.name="barievbulat"
git config --local user.email=@barievbulat@gmail.com"

git status

git add -A

git commit -a -m"comment"

git log

git remote add origin https://github.com/barievbulat/git_learn.git

git branch -M main

git push -u origin main

git clone https://github.com/barievbulat/git_learn.git shared_project
